let oldRender=render;render=function(){state.jobs.forEach(sync8);if(view==='dashboard')dashboard();if(view==='planner')planner();if(view==='machines')machines();if(view==='orders')orders();if(view==='shopfloor')shopfloor()};
dashboard=function(){let open=state.jobs.filter(j=>js8(j)!=='done'),active=all8().filter(x=>x.o.status==='running').length,unp=all8().filter(x=>x.o.status!=='done'&&(!x.o.m||!x.o.start)).length,lateN=open.filter(late8).length,total=all8().filter(x=>x.o.status!=='done').reduce((s,x)=>s+od8(x.o,x.j),0),blocked=open.filter(j=>rr8(next8(j),j).color!=='green').length;let ms=MACHINES.map(m=>{let q=all8().filter(x=>x.o.m===m.id&&x.o.status!=='done').sort((a,b)=>(a.o.start||'9').localeCompare(b.o.start||'9')),x=q.find(x=>['running','setup','paused'].includes(x.o.status))||q[0];return`<div class="machine"><div class="head"><div><h3>${m.name}</h3><span class="tiny muted">${m.type}</span></div>${x?`<span class="badge ${badge(x.o.status)}">${statusText(x.o.status)}</span>`:'<span class="badge green">Frei</span>'}</div>${x?`<div class="listrow" onclick="openJob('${x.j.id}','operations','${x.o.id}')"><div><b>${x.j.id} · ${x.j.name}</b><small>${x.o.id} · ${x.o.name} · ${fmtDT(x.o.start)}</small></div>${rb8(x.o,x.j)}</div>`:'<div class="empty">Keine Arbeit eingeplant</div>'}</div>`}).join('');let due=[...open].sort((a,b)=>dnum8(a)-dnum8(b)).slice(0,6).map(j=>`<div class="listrow" onclick="openJob('${j.id}')"><div><b>${j.id} · ${j.name}</b><small>${cops8(j).length} CNC-AG · ${j.qty} Stk.</small></div><span class="badge ${late8(j)?'red':''}">${fmtDate(j.due)}</span></div>`).join('');root.innerHTML=`<div class="kpis"><div class="kpi kpi-link" onclick="openOrderFilter('open')"><small>Offene Aufträge</small><strong>${open.length}</strong><span class="go">Aufträge anzeigen ›</span></div><div class="kpi kpi-link" onclick="openOrderFilter('running')"><small>Maschinen aktiv</small><strong>${active}</strong><span class="go">Laufende AG ›</span></div><div class="kpi kpi-link" onclick="openOrderFilter('unplanned')"><small>Ungeplante AG</small><strong>${unp}</strong><span class="go">Ungeplante anzeigen ›</span></div><div class="kpi kpi-link" onclick="openOrderFilter('blocked')"><small>Nicht rüstbereit</small><strong>${blocked}</strong><span class="go">Blockierte anzeigen ›</span></div><div class="kpi kpi-link" onclick="openOrderFilter('late')"><small>Terminrisiken</small><strong>${lateN}</strong><span class="go">Risiken anzeigen ›</span></div><div class="kpi kpi-link" onclick="openOrderFilter('capacity')"><small>Offene Maschinenzeit</small><strong>${fmtMin(total)}</strong><span class="go">Nach Zeit sortieren ›</span></div></div><div class="dashgrid"><section class="panel"><div class="head"><h2>Maschinenstatus</h2><button class="btn small" onclick="show('machines')">Alle Maschinen</button></div><div class="machinegrid">${ms}</div></section><section class="panel"><div class="head"><h2>Nächste Liefertermine</h2></div>${due}</section></div><div class="modules"><div class="module" onclick="show('planner')"><div class="icon">▤</div><h3>Zeitplanung</h3><p>Arbeitsgänge aller CNC-Maschinen mit Rüstbereitschaft und Terminwarnung.</p></div><div class="module" onclick="show('machines')"><div class="icon">⚙</div><h3>Maschinenübersicht</h3><p>Konkrete Arbeitsgänge je Maschine und direkter Einstieg in den Auftrag.</p></div><div class="module" onclick="show('orders')"><div class="icon">☷</div><h3>Fertigungsaufträge</h3><p>Arbeitsgangkette, Aufspannung, Werkzeuge, Programm und Prüfung.</p></div><div class="module" onclick="show('shopfloor')"><div class="icon">▣</div><h3>Maschinenansicht</h3><p>Aktueller Arbeitsgang, Rüstampel und Rückmeldung an der Maschine.</p></div></div>`};